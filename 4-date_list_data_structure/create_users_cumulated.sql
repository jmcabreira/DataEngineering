CREATE TABLE `silent-scanner-465121-j7.jmc_data.users_cumulated` (
              user_id STRING, 
              dates_active ARRAY<STRING>, -- the list of dates where the user was active
              date DATE, -- the current date for the user
                )


